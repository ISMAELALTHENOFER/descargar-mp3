{
  "name": "descargar-mp3",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm run dev -w packages/server",
    "dev:web": "npm run dev -w packages/web",
    "build": "npm run build -w packages/web && npm run build -w packages/server",
    "start": "npm run start -w packages/server",
    "lint": "npm run lint -w packages/server && npm run lint -w packages/web",
    "clean": "npm run clean -w packages/server && npm run clean -w packages/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
